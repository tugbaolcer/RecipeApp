<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="vm"
            type="com.tugbaolcer.recipeapp.presentation.hub.register.RegisterViewModel" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="@color/light_gray"
        android:padding="16dp"
        tools:context=".ui.hub.register.RegisterFragment">

        <com.tugbaolcer.recipeapp.utils.CustomTextInputLayout
            android:id="@+id/til_email"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            app:til_hint="@string/Label.Email" />

        <com.tugbaolcer.recipeapp.utils.CustomTextInputLayout
            android:id="@+id/til_password"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            app:til_hint="@string/Label.Password"
            app:til_show_password="true"/>

        <com.tugbaolcer.recipeapp.utils.CustomTextInputLayout
            android:id="@+id/til_repassword"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            app:til_hint="@string/Label.RePassword"
            app:til_show_password="true"/>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_register"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:backgroundTint="@color/blue"
            android:layout_marginTop="8dp"
            android:text="@string/Button.Label.Register" />

    </LinearLayout>
</layout>
